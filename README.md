Задание 6

**Проблема:** при инициализации объекта “видео” (экземпляр класса `Video`), пользователь может передать некорректную строку с id видео. 

Внедрите в код блоки `try/except` , чтобы отловить исключение. 

Если пользователь передал id, с которым невозможно получить данные о видео по API, то у экземпляра инициализируется только свойство `video_id`, а остальные поля принимают значение `None`.

